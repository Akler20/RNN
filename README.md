# RNN
Implemented a Recurrent Neural Network (RNN) from scratch in Python without using deep learning frameworks such as TensorFlow or PyTorch. The project demonstrates how sequential data is processed using hidden states that retain information from previous time steps, enabling the network to learn temporal patterns and dependencies in data.
